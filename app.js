const express = require('exoress')
const app = express()
const port = 8000
app.listen(port, ()=>{
    console.log( `Server is running ${port}`)
})
console.log('Waheed')
console.log(module)
console.log('Waheed')